# Tasks — Structured Scorecards & Interview Kits

## Task 1: Database Migrations and Models

- [ ] 1.1 Create migration `2025_01_06_000001_create_interview_kits_table.php` with UUID PK, `pipeline_stage_id` FK to `pipeline_stages` (cascade delete), `name` (string 255), `description` (text nullable), timestamps, and index on `pipeline_stage_id`
- [ ] 1.2 Create migration `2025_01_06_000002_create_interview_kit_questions_table.php` with UUID PK, `interview_kit_id` FK to `interview_kits` (cascade delete), `text` (text), `category` (string 20), `sort_order` (unsigned smallint default 0), `scoring_rubric` (text nullable), timestamps, indexes on `interview_kit_id` and `(interview_kit_id, sort_order)`
- [ ] 1.3 Create migration `2025_01_06_000003_create_scorecards_table.php` with UUID PK, `interview_id` FK to `interviews` (cascade delete), `submitted_by` FK to `users` (cascade delete), `overall_rating` (unsigned smallint), `overall_recommendation` (string 20), `notes` (text nullable), `submitted_at` (timestamp), timestamps, unique constraint on `(interview_id, submitted_by)`, indexes on `interview_id` and `submitted_by`
- [ ] 1.4 Create migration `2025_01_06_000004_create_scorecard_criteria_table.php` with UUID PK, `scorecard_id` FK to `scorecards` (cascade delete), `question_text` (text), `category` (string 20), `sort_order` (unsigned smallint default 0), `rating` (unsigned smallint), `notes` (text nullable), timestamps, indexes on `scorecard_id` and `(scorecard_id, sort_order)`
- [ ] 1.5 Create `InterviewKit` model in `backend/app/Models/InterviewKit.php` with HasFactory, HasUuid traits, fillable fields (`pipeline_stage_id`, `name`, `description`), `pipelineStage()` BelongsTo relationship, and `questions()` HasMany relationship ordered by `sort_order`
- [ ] 1.6 Create `InterviewKitQuestion` model in `backend/app/Models/InterviewKitQuestion.php` with HasFactory, HasUuid traits, fillable fields (`interview_kit_id`, `text`, `category`, `sort_order`, `scoring_rubric`), `sort_order` integer cast, and `interviewKit()` BelongsTo relationship
- [ ] 1.7 Create `Scorecard` model in `backend/app/Models/Scorecard.php` with HasFactory, HasUuid traits, fillable fields (`interview_id`, `submitted_by`, `overall_rating`, `overall_recommendation`, `notes`, `submitted_at`), casts for `overall_rating` (integer) and `submitted_at` (datetime), `interview()` BelongsTo, `submitter()` BelongsTo (User via `submitted_by`), and `criteria()` HasMany ordered by `sort_order`
- [ ] 1.8 Create `ScorecardCriterion` model in `backend/app/Models/ScorecardCriterion.php` with HasFactory, HasUuid traits, fillable fields (`scorecard_id`, `question_text`, `category`, `sort_order`, `rating`, `notes`), casts for `sort_order` and `rating` (integer), and `scorecard()` BelongsTo relationship
- [ ] 1.9 Run migrations and verify all four tables are created with correct columns, indexes, and foreign keys

## Task 2: Default Kit Templates Config

- [ ] 2.1 Create `backend/config/interview_kit_templates.php` returning an array with four template keys (`phone_screen`, `technical_interview`, `culture_fit`, `final_round`), each containing `name`, `description`, and `questions` array with `text`, `category`, and `scoring_rubric` fields as specified in the design document

## Task 3: Interview Kit Backend (Service, Controller, Requests)

- [ ] 3.1 Create `CreateInterviewKitRequest` in `backend/app/Http/Requests/CreateInterviewKitRequest.php` with validation rules: `name` required string max 255, `description` sometimes nullable string max 2000, `questions` required array min 1, `questions.*.text` required string max 1000, `questions.*.category` required string in:technical,behavioral,cultural,experience, `questions.*.sort_order` required integer min 0, `questions.*.scoring_rubric` sometimes nullable string max 2000
- [ ] 3.2 Create `UpdateInterviewKitRequest` in `backend/app/Http/Requests/UpdateInterviewKitRequest.php` with validation rules: `name` sometimes string max 255, `description` sometimes nullable string max 2000, `questions` sometimes array min 1, `questions.*.text` required_with:questions string max 1000, `questions.*.category` required_with:questions string in:technical,behavioral,cultural,experience, `questions.*.sort_order` required_with:questions integer min 0, `questions.*.scoring_rubric` sometimes nullable string max 2000
- [ ] 3.3 Create `InterviewKitService` in `backend/app/Services/InterviewKitService.php` with methods: `listForJob(jobId, tenantId)` returning kits grouped by stage with question counts, `getDetail(kitId, tenantId)` returning kit with questions, `create(data, stageId, jobId, tenantId)` validating stage-job-tenant ownership and creating kit with questions, `update(kit, data)` replacing questions, `delete(kit)` removing kit and questions, `getDefaultTemplates(stageName)` reading from config, `createFromTemplate(templateKey, stageId, jobId, tenantId)` copying template as independent kit
- [ ] 3.4 Create `InterviewKitController` in `backend/app/Http/Controllers/InterviewKitController.php` with methods: `listForJob(request, jobId)`, `show(request, id)`, `store(CreateInterviewKitRequest, jobId, stageId)`, `update(UpdateInterviewKitRequest, id)`, `destroy(request, id)`, `templates(request)`, `createFromTemplate(request, jobId, stageId)` — all with tenant scoping and proper response formatting
- [ ] 3.5 Register interview kit routes in `backend/routes/api.php` inside the `havenhr.auth + tenant.resolve` middleware group: GET `/jobs/{jobId}/interview-kits` (rbac:jobs.view), POST `/jobs/{jobId}/stages/{stageId}/interview-kits` (rbac:pipeline.manage), POST `/jobs/{jobId}/stages/{stageId}/interview-kits/from-template` (rbac:pipeline.manage), GET `/interview-kits/{id}` (rbac:jobs.view), PUT `/interview-kits/{id}` (rbac:pipeline.manage), DELETE `/interview-kits/{id}` (rbac:pipeline.manage), GET `/interview-kit-templates` (rbac:jobs.view)

## Task 4: Scorecard Backend (Service, Controller, Requests)

- [ ] 4.1 Create `SubmitScorecardRequest` in `backend/app/Http/Requests/SubmitScorecardRequest.php` with validation rules: `overall_rating` required integer min 1 max 5, `overall_recommendation` required string in:strong_no,no,mixed,yes,strong_yes, `notes` sometimes nullable string max 5000, `criteria` sometimes array, `criteria.*.question_text` required_with:criteria string max 1000, `criteria.*.category` required_with:criteria string in:technical,behavioral,cultural,experience, `criteria.*.sort_order` required_with:criteria integer min 0, `criteria.*.rating` required_with:criteria integer min 1 max 5, `criteria.*.notes` sometimes nullable string max 2000
- [ ] 4.2 Create `UpdateScorecardRequest` in `backend/app/Http/Requests/UpdateScorecardRequest.php` with validation rules: `overall_rating` sometimes integer min 1 max 5, `overall_recommendation` sometimes string in:strong_no,no,mixed,yes,strong_yes, `notes` sometimes nullable string max 5000, `criteria` sometimes array, `criteria.*.question_text` required_with:criteria string max 1000, `criteria.*.category` required_with:criteria string in:technical,behavioral,cultural,experience, `criteria.*.sort_order` required_with:criteria integer min 0, `criteria.*.rating` required_with:criteria integer min 1 max 5, `criteria.*.notes` sometimes nullable string max 2000
- [ ] 4.3 Create `ScorecardService` in `backend/app/Services/ScorecardService.php` with methods: `getScorecardForm(interviewId, tenantId)` returning criteria from the kit linked to the interview's pipeline stage, `submit(data, interviewId, userId, tenantId)` validating interview is completed and no duplicate exists then creating scorecard with criteria snapshots, `update(scorecard, data)` updating ratings/notes/criteria, `getDetail(scorecardId, tenantId)` returning scorecard with criteria, `getSummary(applicationId, tenantId)` computing total count, average overall rating, recommendation distribution, per-criterion averages, and individual interviewer entries, `listForInterview(interviewId, tenantId)` returning all scorecards for an interview
- [ ] 4.4 Create `ScorecardController` in `backend/app/Http/Controllers/ScorecardController.php` with methods: `form(request, interviewId)`, `store(SubmitScorecardRequest, interviewId)`, `show(request, id)`, `update(UpdateScorecardRequest, id)` with ownership check (submitted_by must match auth user), `listForInterview(request, interviewId)`, `summary(request, appId)` — all with tenant scoping and proper response formatting
- [ ] 4.5 Register scorecard routes in `backend/routes/api.php` inside the `havenhr.auth + tenant.resolve` middleware group: GET `/interviews/{interviewId}/scorecard-form` (rbac:applications.view), POST `/interviews/{interviewId}/scorecard` (rbac:applications.manage), GET `/interviews/{interviewId}/scorecards` (rbac:applications.view), GET `/scorecards/{id}` (rbac:applications.view), PUT `/scorecards/{id}` (rbac:applications.manage), GET `/applications/{appId}/scorecard-summary` (rbac:applications.view)

## Task 5: Frontend TypeScript Types and API Clients

- [ ] 5.1 Create `frontend/src/types/interviewKit.ts` with types: `QuestionCategory`, `InterviewKitQuestion`, `InterviewKit`, `InterviewKitListItem`, `StageKits`, `InterviewKitTemplate`, `CreateInterviewKitPayload`, `UpdateInterviewKitPayload` as specified in the design document
- [ ] 5.2 Create `frontend/src/types/scorecard.ts` with types: `OverallRecommendation`, `ScorecardCriterion`, `Scorecard`, `ScorecardFormCriterion`, `ScorecardForm`, `ScorecardSummary`, `SubmitScorecardPayload`, `UpdateScorecardPayload` as specified in the design document
- [ ] 5.3 Create `frontend/src/lib/interviewKitApi.ts` with functions: `listInterviewKitsForJob(jobId)`, `getInterviewKitDetail(kitId)`, `createInterviewKit(jobId, stageId, payload)`, `updateInterviewKit(kitId, payload)`, `deleteInterviewKit(kitId)`, `listInterviewKitTemplates()`, `createInterviewKitFromTemplate(jobId, stageId, templateKey)` — all using `apiClient` from `@/lib/api`
- [ ] 5.4 Create `frontend/src/lib/scorecardApi.ts` with functions: `getScorecardForm(interviewId)`, `submitScorecard(interviewId, payload)`, `getScorecardDetail(scorecardId)`, `updateScorecard(scorecardId, payload)`, `listScorecardsForInterview(interviewId)`, `getScorecardSummary(applicationId)` — all using `apiClient` from `@/lib/api`

## Task 6: Interview Kit Management UI

- [ ] 6.1 Create `InterviewKitManager` component in `frontend/src/components/interviews/InterviewKitManager.tsx` that fetches and displays all interview kits for a job posting grouped by pipeline stage, with "Add Kit" button per stage, kit cards showing name/description/question count, and edit/delete actions with confirmation dialog
- [ ] 6.2 Create `InterviewKitForm` component in `frontend/src/components/interviews/InterviewKitForm.tsx` with form fields for name, description, and dynamic question list (add/remove/reorder questions with up/down buttons), category dropdown per question, optional scoring rubric textarea, client-side validation, and template selection dropdown for pre-filling from defaults
- [ ] 6.3 Integrate `InterviewKitManager` into the job detail page, rendering it as a section that displays when viewing a job posting's details, passing the `jobId` and pipeline stages as props

## Task 7: Scorecard Submission UI

- [ ] 7.1 Create `ScorecardSubmissionModal` component in `frontend/src/components/interviews/ScorecardSubmissionModal.tsx` that fetches the scorecard form via `getScorecardForm`, renders criteria with star rating selectors (1–5) and notes textareas, overall rating stars, recommendation button group (Strong No/No/Mixed/Yes/Strong Yes), submit/cancel buttons, inline validation errors, success confirmation, focus trap, and Escape to close with proper ARIA attributes
- [ ] 7.2 Create `ScorecardDetailView` component in `frontend/src/components/interviews/ScorecardDetailView.tsx` that displays a single scorecard's full details: submitter name, submission date, overall rating as stars, recommendation badge, per-criterion list with question text, category badge, rating stars, and notes, with an edit button visible only to the scorecard's submitter

## Task 8: Scorecard Summary in SlideOverPanel

- [ ] 8.1 Create `ScorecardSummarySection` component in `frontend/src/components/pipeline/ScorecardSummarySection.tsx` that fetches the scorecard summary via `getScorecardSummary(applicationId)`, displays average overall rating as stars, total scorecard count, recommendation distribution as colored badges, side-by-side interviewer comparison entries, expandable detail view per interviewer, loading skeleton, and empty state message
- [ ] 8.2 Integrate `ScorecardSummarySection` into the existing `SlideOverPanel` component by adding it as a new section below the Interviews section, passing the `applicationId` prop
- [ ] 8.3 Add "Submit Scorecard" button to the `InterviewList` component for completed interviews that opens the `ScorecardSubmissionModal`, and show a "View Scorecard" link for interviews that already have a scorecard from the current user